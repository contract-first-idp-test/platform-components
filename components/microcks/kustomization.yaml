apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - external-secret.yaml
  # Workloads are rendered by the official Microcks chart. This source only
  # owns companion resources that the chart consumes.
  
