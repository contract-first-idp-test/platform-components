apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: contract-first-idp-platform-configuration
  description: Mutable configuration for this Contract-First IDP installation.
spec:
  type: file
  targets:
    - ./configuration/catalog-info.yaml
